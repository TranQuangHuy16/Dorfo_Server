http {
    server {
        listen 80;
        server_name dorfo.duckdns.org;

        # Đường dẫn cho Certbot ACME challenge
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
            default_type "text/plain";
        }

        # Chuyển hướng tất cả request khác sang HTTPS
        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl http2;
        server_name dorfo.duckdns.org;

        ssl_certificate /etc/letsencrypt/live/dorfo.duckdns.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/dorfo.duckdns.org/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        location / {
            # Nếu backend là Docker container trong cùng network
            proxy_pass http://backend:80;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }
}
